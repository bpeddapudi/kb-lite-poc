# Mapbox public token (pk.…) — https://account.mapbox.com/ — required for maps locally and in CI
# Vite only exposes vars prefixed with VITE_
# For GitHub Pages: add the same name as a repository secret (Actions) so the deploy build can read it.
VITE_MAPBOX_TOKEN=
